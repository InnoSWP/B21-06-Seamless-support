import React from "react";

const Footer = (props) => {
    return(
        <div className={'footer'}>
            <form className={'footer-form'}>
                <input />
            </form>
        </div>
    );
}

export default Footer;